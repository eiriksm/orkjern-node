[Service]
ExecStart=/usr/bin/npm start --prefix={{logger_install}}
Restart=always
StandardOutput=syslog
StandardError=syslog
SyslogIdentifier=node
User={{node_user}}
Group={{node_user}}
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target
